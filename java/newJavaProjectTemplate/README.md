# info
This folder contains basic project configurations files for java like:
.gitignore
 
# Usage as a part of parent branch and projects as subbranches 
* git clone --single-branch -b ${remoteBranchName} ${projectPath} ${remoteBranchName}-local
* git branch --track ${branchPath-projectName}
* git checkout ${branchPath-projectName}
* mv newJavaProject ${projectName}
* git push --set-upstream  origin ${branchPaht-ProjectName}
